- breadcrumb :build_job, @build_job

- content_for :navigation_actions do
  = render partial: 'actions'

- if @build_job.completed? || @build_job.failure?
  = render partial: 'finshed'
- else
  = render partial: 'running'
