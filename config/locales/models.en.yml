en:
  activerecord:
    attributes:
      build: &build_attributes
        date: Date
        result: Result
        error_message: Error Message
    models:
      build: Build
build:
  fields:
    <<: &build_attributes
